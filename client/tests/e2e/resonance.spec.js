/// <reference types="cypress" />

const testId = (id) => `[data-testid="${id}"]`

describe('resonance', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000')
  })

  describe('resonance', () => {})
})
